{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Run Go (Golang) file",
			"type": "go",
			"request": "launch",
			"mode": "auto",
			"cwd": "${workspaceFolder}",
			"program": "${file}"
		}
	]
}
